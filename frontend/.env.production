# Used by `vite build` (web deploy + Capacitor APK). The dev server ignores this
# file and proxies /api to the local backend instead (see vite.config.ts).
VITE_API_BASE=https://supari-service.onrender.com/api
